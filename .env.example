# Aiven MySQL Service URI (mysql://user:pass@host:port/dbname)
AIVEN_MYSQL_URI=

# Optional: paste full Aiven CA cert PEM here to avoid checking in ca.pem.
# If unset, the api/seed code falls back to ./ca.pem in the project root.
AIVEN_CA_CERT=

# Long random string for signing JWTs.
JWT_SECRET=
